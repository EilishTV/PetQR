// Profile logic (placeholder for future auth)
console.log("Profile loaded");

// Example future actions
document.querySelectorAll(".profile-item").forEach(item=>{
  item.onclick=()=>alert(item.textContent.trim());
});
